import 'package:flutter/material.dart';

class BellsWidget extends StatelessWidget {
  const BellsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
